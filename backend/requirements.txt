# Core dependencies — pin versions for reproducibility

# Broker connectivity
ib_insync>=0.9.86          # IBKR async adapter

# Configuration and validation
pydantic>=2.8.0
pydantic-settings>=2.5.0
PyYAML>=6.0.1

# Logging
structlog>=24.4.0

# Numerical
numpy>=1.26.0,<2.0
scipy>=1.12.0,<2.0
pandas>=2.1.0,<3.0

# Storage
pyarrow>=14.0.0            # Parquet support

# Historical data
yfinance>=0.2.40           # Yahoo Finance — stocks/indices backtest data

# Dashboard (web server)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Plotting (for surface visualization)
matplotlib>=3.8.0
plotly>=5.18.0             # Interactive charts for dashboard

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Type checking
mypy>=1.7.0
